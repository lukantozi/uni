# Python Quiz

**50 questions** | Generated: 2026-06-01 08:02

---

## Q1 `gate3_encapsulation_dunder` / `layer_b`

```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __eq__(self, other):
        return self.x == other.x and self.y == other.y
p1 = Point(1, 2)
p2 = Point(1, 2)
print(p1 == p2)
print(p1 is p2)
try:
    s = {p1, p2}
except TypeError:
    print('TypeError')
```

## Q2 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_in(self):
        self.assertIn('a', ['a', 'b', 'c'])
    def test_not_in(self):
        self.assertNotIn('z', ['a', 'b', 'c'])
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q3 `gate3_encapsulation_dunder` / `layer_b`

```python
class Pair:
    def __init__(self, a, b):
        self.a = a
        self.b = b
    def __iter__(self):
        return iter([self.a, self.b])
    def __repr__(self):
        return f'Pair({self.a}, {self.b})'
p = Pair(10, 20)
x, y = p
print(x, y)
print(list(p))
```

## Q4 `gate1_oop_basics` / `layer_b`

```python
class Foo:
    pass

f = Foo()
print(type(f) == Foo)
print(type(f) == type(Foo))
```

## Q5 `gate1_oop_basics` / `layer_c`

```python
class A:
    x = 10
    def __init__(self): self.x = 20
a = A()
print(a.x)
print(A.x)
```

## Q6 `gate2_inheritance` / `layer_b`

```python
class Animal:
    legs = 4
    def __init__(self, name):
        self.name = name

class Bird(Animal):
    legs = 2

class Dog(Animal):
    pass

print(Dog.legs)
print(Bird.legs)
b = Bird('Tweety')
print(b.legs)
print(isinstance(b, Animal))
```

## Q7 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def setUp(self):
        self.x = 10
    def test_one(self):
        self.x = 99
        self.assertEqual(self.x, 99)
    def test_two(self):
        self.assertEqual(self.x, 10)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q8 `gate4_unit_testing` / `layer_a`

```python
import unittest
class T(unittest.TestCase):
    def test_fail(self):
        self.assertEqual(1 + 1, 3)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q9 `gate2_inheritance` / `layer_c`

```python
class Animal:
    def __init__(self, name): self.name = name
    def __str__(self): return f'Animal({self.name})'
class Dog(Animal): pass
print(Dog('Rex'))
```

## Q10 `gate3_encapsulation_dunder` / `layer_b`

```python
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __add__(self, other):
        return Vector(self.x + other.x, self.y + other.y)
    def __repr__(self):
        return f'({self.x},{self.y})'
    def __eq__(self, other):
        return self.x == other.x and self.y == other.y
v3 = Vector(1, 2) + Vector(3, 4)
print(v3)
print(v3 == Vector(4, 6))
```

## Q11 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([1,2,3,4,5,6])[::3])
```

## Q12 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([1,2,3])[1])
```

## Q13 `gate4_unit_testing` / `layer_a`

```python
import unittest
class T(unittest.TestCase):
    def test_true(self):
        self.assertTrue(5 > 3)
    def test_false(self):
        self.assertFalse(2 > 10)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q14 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.sum(np.array([1,2,3,4,5])))
```

## Q15 `gate1_oop_basics` / `layer_b`

```python
class Person:
    def __init__(self, name):
        self.name = name

p1 = Person('Alice')
p2 = p1
p2.name = 'Bob'
print(p1.name)
```

## Q16 `gate2_inheritance` / `layer_c`

```python
class A:
    def hello(self): return 'A'
class B(A): pass
print(B().hello())
```

## Q17 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __init__(self):
        self.__x = 10
a = A()
a.__x = 99
print(vars(a))
```

## Q18 `gate1_oop_basics` / `layer_a`

```python
class MyClass:
    pass

obj = MyClass()
obj.color = 'red'
print(obj.color)
```

## Q19 `gate2_inheritance` / `layer_a`

```python
class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed

d = Dog('Rex', 'Labrador')
print(type(d).__name__)
print(isinstance(d, Animal))
```

## Q20 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3])\nb = np.array([10, 20, 30])\nprint(a + b)
```

## Q21 `gate6_pandas_json` / `layer_c`

```python
df.dropna() — drops rows or columns by default?
```

## Q22 `gate5_numpy` / `layer_c`

```python
import numpy as np\na = np.array([3,1,2])\nb = np.sort(a)\nprint(a)
```

## Q23 `gate6_pandas_json` / `layer_c`

```python
json.loads('{"x": 1}') returns what type?
```

## Q24 `gate6_pandas_json` / `layer_c`

```python
df.loc['x', 'a'] — uses position or label?
```

## Q25 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __init__(self):
        self.__x = 10
a = A()
a.__x = 99
print(a.__x)
print(a._A__x)
```

## Q26 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'val': [1,2,3,4,5]})
df2 = df[df['val'] > 2]
print(df2.reset_index(drop=True)['val'].tolist())
```

## Q27 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3]})
print(type(df['a']))
```

## Q28 `gate2_inheritance` / `layer_b`

```python
class A:
    def __init__(self):
        self.x = 'A'

class B(A):
    def __init__(self):
        self.y = 'B'

b = B()
print(b.y)
print(b.x)
```

## Q29 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_is(self):
        a = None
        self.assertIs(a, None)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q30 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.arange(1, 10).reshape(3, 3)\nprint(a[1:, 1:])
```

## Q31 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5, 6])\nb = a[2:5]\nb[0] = 99\nprint(a)
```

## Q32 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.unique(np.array([1,1,2,3,3,4])))
```

## Q33 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.vstack((np.array([1,2]), np.array([3,4]))).shape)
```

## Q34 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'score': [40,70,85]})
df['pass'] = df['score'] >= 60
print(df['pass'].tolist())
```

## Q35 `gate1_oop_basics` / `layer_c`

```python
class A:
    x = 1
a = A()
print(a.x)
```

## Q36 `gate2_inheritance` / `layer_a`

```python
class A:
    x = 'A'

class B(A):
    pass

class C(A):
    x = 'C'

print(B.x)
print(C.x)
```

## Q37 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([[1,2],[3,4]]).ndim)
```

## Q38 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self, n): self.n = n
class B(A):
    def __init__(self, n, m):
        super().__init__(n)
        self.m = m
b = B(1, 2)
print(b.n, b.m)
```

## Q39 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3], 'b': [4,5,6]})
print(df.iloc[0])
```

## Q40 `gate4_unit_testing` / `layer_a`

```python
import unittest
class T(unittest.TestCase):
    def test_ne(self):
        self.assertNotEqual(1, 2)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q41 `gate3_encapsulation_dunder` / `layer_a`

```python
class MyNum:
    def __init__(self, val):
        self.val = val
    def __bool__(self):
        return self.val != 0
print(bool(MyNum(5)))
print(bool(MyNum(0)))
```

## Q42 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self): self.x = 1
class B(A):
    def __init__(self): self.y = 2
b = B()
print(b.y)
print(b.x)
```

## Q43 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_raises_catches_subclass(self):
        with self.assertRaises(Exception):
            raise ValueError('sub')
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q44 `gate6_pandas_json` / `layer_c`

```python
df.dropna() — returns new DataFrame or modifies in place?
```

## Q45 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_almost(self):
        self.assertAlmostEqual(0.1 + 0.2, 0.3, places=5)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q46 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_raises_no_block(self):
        self.assertRaises(ValueError, int, 'abc')
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q47 `gate3_encapsulation_dunder` / `layer_a`

```python
class Box:
    def __init__(self, items):
        self.items = items
    def __len__(self):
        return len(self.items)
b = Box([1, 2, 3])
print(len(b))
```

## Q48 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_one(self): self.assertEqual(1, 1)
    def test_two(self): self.assertEqual(2, 2)
    def test_three(self): self.assertEqual(1, 2)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q49 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([[1,2],[3,4]]).shape)
```

## Q50 `gate3_encapsulation_dunder` / `layer_a`

```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __lt__(self, other):
        return self.x < other.x
p1 = Point(1, 5)
p2 = Point(3, 2)
print(p1 < p2)
```

---

## Answers

**Q1:** `True
False
TypeError`  
**Q2:** `True`  
**Q3:** `10 20
[10, 20]`  
**Q4:** `True
False`  
**Q5:** `20
10`  
**Q6:** `4
2
2
True`  
**Q7:** `True`  
**Q8:** `False`  
**Q9:** `Animal(Rex)`  
**Q10:** `(4,6)
True`  
**Q11:** `[1 4]`  
**Q12:** `2`  
**Q13:** `2
True`  
**Q14:** `15`  
**Q15:** `Bob`  
**Q16:** `A`  
**Q17:** `{'_A__x': 10, '__x': 99}`  
**Q18:** `red`  
**Q19:** `Dog
True`  
**Q20:** `[11 22 33]`  
**Q21:** `rows (axis=0)`  
**Q22:** `[3 1 2]`  
**Q23:** `dict`  
**Q24:** `label`  
**Q25:** `99
10`  
**Q26:** `[3, 4, 5]`  
**Q27:** `<class 'pandas.core.series.Series'>`  
**Q28:** `B
AttributeError: 'B' object has no attribute 'x'`  
**Q29:** `True`  
**Q30:** `[[5 6]\n [8 9]]`  
**Q31:** `[ 1  2 99  4  5  6]`  
**Q32:** `[1 2 3 4]`  
**Q33:** `(2, 2)`  
**Q34:** `[False, True, True]`  
**Q35:** `1`  
**Q36:** `A
C`  
**Q37:** `2`  
**Q38:** `1 2`  
**Q39:** `a    1
b    4
Name: 0, dtype: int64`  
**Q40:** `True`  
**Q41:** `True
False`  
**Q42:** `2
AttributeError`  
**Q43:** `True`  
**Q44:** `returns new DataFrame`  
**Q45:** `True`  
**Q46:** `True`  
**Q47:** `3`  
**Q48:** `3
False`  
**Q49:** `(2, 2)`  
**Q50:** `True`  
