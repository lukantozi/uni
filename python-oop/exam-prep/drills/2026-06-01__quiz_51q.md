# Python Quiz

**51 questions** | Generated: 2026-06-01 10:00

---

## Q1 `gate6_pandas_json` / `layer_c`

```python
df.iloc[0:2] — is endpoint inclusive?
```

## Q2 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.hstack((np.array([[1],[2]]), np.array([[3],[4]]))).shape)
```

## Q3 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_true(self):
        self.assertTrue(1)
        self.assertTrue([1])
        self.assertTrue('a')
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q4 `gate6_pandas_json` / `layer_c`

```python
df[['a']] returns what type?
```

## Q5 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
import json
d = {'x': 1, 'y': 2}
s = json.dumps(d)
d2 = json.loads(s)
print(d2['x'] + d2['y'])
```

## Q6 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3], 'b': [4,5,6]})
print(df.iloc[:2, :1])
```

## Q7 `gate6_pandas_json` / `layer_c`

```python
pd.DataFrame({'a':[None,None]}).dropna().shape
```

## Q8 `gate1_oop_basics` / `layer_c`

```python
class A:
    count = 0
    def __init__(self): self.count += 1
a = A()
print(A.count)
print(a.count)
```

## Q9 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
import json
data = '{"name": "Alice", "age": 30}'
d = json.loads(data)
print(d['name'])
```

## Q10 `gate2_inheritance` / `layer_a`

```python
class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    pass

d = Dog('Rex')
print(d.name)
```

## Q11 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3], 'b': [4,5,6]}, index=['x','y','z'])
print(df.loc['y', 'b'])
```

## Q12 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([[1, 2, 3], [4, 5, 6]])\nprint(a.shape)\nprint(a.ndim)
```

## Q13 `gate2_inheritance` / `layer_c`

```python
class Animal:
    def speak(self): return '...'
class Dog(Animal):
    def speak(self): return 'Woof'
class Cat(Animal):
    def speak(self): return 'Meow'
for a in [Dog(), Cat(), Animal()]:
    print(a.speak())
```

## Q14 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
import json
data = [{"name": "Alice"}, {"name": "Bob"}]
s = json.dumps(data)
print(type(s))
print(s[0])
```

## Q15 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3]})
print(df['a'][0])
print(type(df['a'][0]))
```

## Q16 `gate1_oop_basics` / `layer_a`

```python
class MyClass:
    pass

obj = MyClass()
obj.color = 'red'
print(obj.color)
```

## Q17 `gate4_unit_testing` / `layer_b`

```python
import unittest
class T(unittest.TestCase):
    def test_empty_string(self):
        self.assertEqual(len(''), 0)
        self.assertFalse('')
    def test_zero(self):
        self.assertEqual(0, 0)
        self.assertFalse(0)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q18 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self): self.val = self.get_val()
    def get_val(self): return 0
class B(A):
    def get_val(self): return 99
print(B().val)
```

## Q19 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([[1, 2], [3, 4]])\nb = np.array([[5, 6], [7, 8]])\nprint(np.hstack((a, b)))
```

## Q20 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self): self.x = 1
class B(A):
    def __init__(self):
        super().__init__()
        self.y = 2
b = B()
print(b.x, b.y)
```

## Q21 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'val': [1,2,3,4,5]})
df2 = df[df['val'] > 2]
print(df2.reset_index(drop=True)['val'].tolist())
```

## Q22 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self, n): self.n = n
class B(A):
    def __init__(self, n, m):
        super().__init__(n)
        self.m = m
b = B(1, 2)
print(b.n, b.m)
```

## Q23 `gate2_inheritance` / `layer_a`

```python
class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed

d = Dog('Rex', 'Labrador')
print(type(d).__name__)
print(isinstance(d, Animal))
```

## Q24 `gate2_inheritance` / `layer_c`

```python
class A:
    def __str__(self): return 'A'
class B(A):
    pass
print(B())
```

## Q25 `gate2_inheritance` / `layer_c`

```python
class Person:
    def __init__(self, name): self.name = name
class Student(Person):
    def __init__(self, name, gpa):
        super().__init__(name)
        self.gpa = gpa
s = Student('Alice', 4.0)
print(s.name, s.gpa)
```

## Q26 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_is(self):
        a = None
        self.assertIs(a, None)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q27 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([[1,2],[3,4]]).sum(axis=0))
```

## Q28 `gate1_oop_basics` / `layer_a`

```python
class Dog:
    def __init__(self, name):
        self.name = name

d = Dog('Rex')
print(d.name)
```

## Q29 `gate4_unit_testing` / `layer_b`

```python
import unittest
class T(unittest.TestCase):
    def test_list(self):
        self.assertEqual([1, 2, 3], [1, 2, 3])
    def test_dict(self):
        self.assertEqual({'a': 1}, {'a': 1})
    def test_identity(self):
        a = [1, 2]
        b = [1, 2]
        self.assertEqual(a, b)
        self.assertFalse(a is b)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q30 `gate3_encapsulation_dunder` / `layer_b`

```python
class Celsius:
    def __init__(self, temp):
        self._temp = temp
    @property
    def temp(self):
        return self._temp
    @temp.setter
    def temp(self, value):
        if value < -273.15:
            raise ValueError('Too cold')
        self._temp = value
c = Celsius(25)
c.temp = -300
```

## Q31 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([3, 1, 4, 1, 5, 9, 2, 6])\nprint(np.sort(a))\nprint(a)
```

## Q32 `gate3_encapsulation_dunder` / `layer_c`

```python
class C:
    def __init__(self, v):
        self._v = v
    @property
    def v(self):
        return self._v
c = C(42)
print(c.v)
```

## Q33 `gate4_unit_testing` / `layer_b`

```python
import unittest
class T(unittest.TestCase):
    def test_in_string(self):
        self.assertIn('ell', 'hello')
    def test_in_dict(self):
        self.assertIn('key', {'key': 1})
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q34 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3], 'b': [4,5,6]})
df['c'] = df['a'] + df['b']
print(df['c'].tolist())
```

## Q35 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_fail(self):
        self.assertEqual(2 + 2, 5)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q36 `gate3_encapsulation_dunder` / `layer_c`

```python
class C:
    def __init__(self, v):
        self._v = v
    @property
    def v(self):
        return self._v
    @v.setter
    def v(self, val):
        self._v = val
c = C(1)
c.v = 5
print(c.v)
```

## Q37 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __repr__(self): return 'repr'
a = A()
print(a)
print(str(a))
```

## Q38 `gate3_encapsulation_dunder` / `layer_a`

```python
class MyList:
    def __init__(self, data):
        self.data = data
    def __getitem__(self, index):
        return self.data[index]
m = MyList([10, 20, 30])
print(m[1])
```

## Q39 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'val': [10,20,30,40]})
print(df[df['val'] > 20])
```

## Q40 `gate2_inheritance` / `layer_a`

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

class Student(Person):
    def __init__(self, name, age, grade):
        super().__init__(name, age)
        self.grade = grade

s = Student('Alice', 20, 'A')
print(isinstance(s, Student))
print(isinstance(s, Person))
```

## Q41 `gate4_unit_testing` / `layer_b`

```python
import unittest
class T(unittest.TestCase):
    def test_wrong_exception(self):
        with self.assertRaises(ValueError):
            raise TypeError('wrong type')
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q42 `gate3_encapsulation_dunder` / `layer_b`

```python
class A:
    def __init__(self, x):
        self.__x = x
    def get(self):
        return self.__x
class B(A):
    def get_b(self):
        return self.__x
b = B(5)
print(b.get())
print(b.get_b())
```

## Q43 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    @unittest.skip('skip me')
    def test_skipped(self):
        self.assertEqual(1, 2)
    def test_normal(self):
        self.assertEqual(1, 1)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q44 `gate2_inheritance` / `layer_c`

```python
class A:
    def method(self): return 'A'
class B(A):
    def method(self):
        return super().method() + '+B'
class C(B):
    def method(self):
        return super().method() + '+C'
print(C().method())
```

## Q45 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5])\nb = a.copy()\nb *= 10\nprint(a)\nprint(b)
```

## Q46 `gate1_oop_basics` / `layer_a`

```python
class Box:
    @staticmethod
    def info():
        return 'I am a box'

print(Box.info())
```

## Q47 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_a(self):
        self.fail('forced failure')
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q48 `gate3_encapsulation_dunder` / `layer_a`

```python
class Circle:
    def __init__(self, r):
        self._r = r
    @property
    def radius(self):
        return self._r
c = Circle(5)
print(c.radius)
```

## Q49 `gate3_encapsulation_dunder` / `layer_b`

```python
class Counter:
    def __init__(self):
        self._n = 0
    def __call__(self, step=1):
        self._n += step
        return self._n
    def __repr__(self):
        return f'Counter({self._n})'
c = Counter()
c(); c(); c(5)
print(c)
```

## Q50 `gate1_oop_basics` / `layer_c`

```python
class A:
    val = []
a1 = A(); a2 = A()
a1.val.append(1)
print(a2.val)
```

## Q51 `gate2_inheritance` / `layer_c`

```python
class Animal:
    def __init__(self, name): self.name = name
    def __str__(self): return f'Animal({self.name})'
class Dog(Animal): pass
print(Dog('Rex'))
```

---

## Answers

**Q1:** `No`  
**Q2:** `(2, 2)`  
**Q3:** `True`  
**Q4:** `DataFrame`  
**Q5:** `3`  
**Q6:** `   a
0  1
1  2`  
**Q7:** `(0, 1)`  
**Q8:** `0
1`  
**Q9:** `Alice`  
**Q10:** `Rex`  
**Q11:** `5`  
**Q12:** `(2, 3)\n2`  
**Q13:** `Woof
Meow
...`  
**Q14:** `<class 'str'>
[`  
**Q15:** `1
<class 'numpy.int64'>`  
**Q16:** `red`  
**Q17:** `True`  
**Q18:** `99`  
**Q19:** `[[1 2 5 6]\n [3 4 7 8]]`  
**Q20:** `1 2`  
**Q21:** `[3, 4, 5]`  
**Q22:** `1 2`  
**Q23:** `Dog
True`  
**Q24:** `A`  
**Q25:** `Alice 4.0`  
**Q26:** `True`  
**Q27:** `[4 6]`  
**Q28:** `Rex`  
**Q29:** `3
True`  
**Q30:** `ValueError: Too cold`  
**Q31:** `[1 1 2 3 4 5 6 9]\n[3 1 4 1 5 9 2 6]`  
**Q32:** `42`  
**Q33:** `True`  
**Q34:** `[5, 7, 9]`  
**Q35:** `False`  
**Q36:** `5`  
**Q37:** `repr
repr`  
**Q38:** `20`  
**Q39:** `   val
2   30
3   40`  
**Q40:** `True
True`  
**Q41:** `False`  
**Q42:** `5
AttributeError`  
**Q43:** `1
True`  
**Q44:** `A+B+C`  
**Q45:** `[1 2 3 4 5]\n[10 20 30 40 50]`  
**Q46:** `I am a box`  
**Q47:** `False`  
**Q48:** `5`  
**Q49:** `Counter(7)`  
**Q50:** `[1]`  
**Q51:** `Animal(Rex)`  
