# Python Quiz

**51 questions** | Generated: 2026-05-31 14:12

---

## Q1 `gate1_oop_basics` / `layer_c`

```python
class A:
    count = 0
    def __init__(self): self.count += 1
a = A()
print(A.count)
print(a.count)
```

## Q2 `gate2_inheritance` / `layer_a`

```python
class Person:
    def __init__(self, name):
        self.name = name

class Student(Person):
    def __init__(self, name, grade):
        super().__init__(name)
        self.grade = grade
    def __str__(self):
        return f'{self.name} - {self.grade}'

s = Student('Bob', 'B+')
print(s)
```

## Q3 `gate2_inheritance` / `layer_a`

```python
class Vehicle:
    def __init__(self, brand):
        self.brand = brand
    def __str__(self):
        return f'Vehicle: {self.brand}'

class Car(Vehicle):
    def __init__(self, brand, model):
        super().__init__(brand)
        self.model = model

c = Car('BMW', 'X5')
print(c)
```

## Q4 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_fail(self):
        self.assertEqual(2 + 2, 5)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q5 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self, x): self.x = x
class B(A):
    def double(self): return self.x * 2
b = B(5)
print(b.double())
```

## Q6 `gate4_unit_testing` / `layer_a`

```python
import unittest
class T(unittest.TestCase):
    def test_none(self):
        x = None
        self.assertIsNone(x)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q7 `gate3_encapsulation_dunder` / `layer_c`

```python
class C:
    def __init__(self, v):
        self._v = v
    @property
    def v(self):
        return self._v
c = C(42)
print(c.v)
```

## Q8 `gate6_pandas_json` / `layer_c`

```python
pd.DataFrame({'a':[1,None,3]}).fillna(0)['a'].tolist()
```

## Q9 `gate3_encapsulation_dunder` / `layer_a`

```python
class Temperature:
    def __init__(self, celsius):
        self._celsius = celsius
    @property
    def fahrenheit(self):
        return self._celsius * 9 / 5 + 32
t = Temperature(100)
print(t.fahrenheit)
```

## Q10 `gate1_oop_basics` / `layer_b`

```python
class Counter:
    count = 0
    def __init__(self): Counter.count += 1
    @classmethod
    def reset(cls): cls.count = 0

Counter(); Counter(); Counter()
print(Counter.count)
Counter.reset()
print(Counter.count)
```

## Q11 `gate6_pandas_json` / `layer_c`

```python
df.iloc[0, 0] — uses position or label?
```

## Q12 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self): self.val = self.get_val()
    def get_val(self): return 0
class B(A):
    def get_val(self): return 99
print(B().val)
```

## Q13 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5, 6])\nb = a[2:5].copy()\nb[0] = 99\nprint(a)
```

## Q14 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_type(self):
        self.assertIsInstance(42, int)
        self.assertIsInstance('hi', str)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q15 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.vstack((np.array([1,2]), np.array([3,4]))).shape)
```

## Q16 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_in(self):
        self.assertIn('a', ['a', 'b', 'c'])
    def test_not_in(self):
        self.assertNotIn('z', ['a', 'b', 'c'])
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q17 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3]})
print(type(df['a']))
```

## Q18 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5])\nresult = np.where(a > 3, 1, 0)\nprint(result)
```

## Q19 `gate5_numpy` / `layer_c`

```python
import numpy as np\na = np.array([3,1,2])\nb = np.sort(a)\nprint(a)
```

## Q20 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __init__(self, data):
        self.data = data
    def __getitem__(self, i):
        return self.data[i]
a = A([10, 20, 30])
print(a[2])
```

## Q21 `gate1_oop_basics` / `layer_b`

```python
class Config:
    debug = False

c = Config()
c.debug = True
print(Config.debug)
print(c.debug)
```

## Q22 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def setUp(self):
        self.x = 10
    def test_one(self):
        self.x = 99
        self.assertEqual(self.x, 99)
    def test_two(self):
        self.assertEqual(self.x, 10)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q23 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([3, 1, 4, 1, 5])\nprint(np.unique(a))
```

## Q24 `gate4_unit_testing` / `layer_a`

```python
import unittest
class T(unittest.TestCase):
    def test_raises(self):
        with self.assertRaises(TypeError):
            x = 'a' + 1
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q25 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_multi(self):
        self.assertEqual(1, 1)
        self.assertEqual(2, 3)
        self.assertEqual(4, 4)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q26 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([[1, 2, 3], [4, 5, 6]])\nprint(a.shape)\nprint(a.ndim)
```

## Q27 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([[1,2],[3,4]]).sum(axis=0))
```

## Q28 `gate2_inheritance` / `layer_c`

```python
class A:
    label = 'A'
class B(A): pass
b = B()
B.label = 'B'
print(b.label)
print(A.label)
```

## Q29 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3], 'b': [4,5,6]})
print(df[['a']].shape)
print(df['a'].shape)
```

## Q30 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self, x): self.x = x
    def __str__(self): return f'A({self.x})'
class B(A):
    def __init__(self, x, y):
        super().__init__(x)
        self.y = y
    def __str__(self): return f'B({self.x},{self.y})'
print(B(1, 2))
```

## Q31 `gate3_encapsulation_dunder` / `layer_b`

```python
class Range:
    def __init__(self, start, end):
        self.start = start
        self.end = end
    def __contains__(self, item):
        return self.start <= item < self.end
r = Range(1, 10)
print(5 in r)
print(10 in r)
print(0 in r)
```

## Q32 `gate3_encapsulation_dunder` / `layer_b`

```python
class MyList:
    def __init__(self, data):
        self._data = data
    def __len__(self):
        return len(self._data)
    def __bool__(self):
        return len(self._data) > 0
m1 = MyList([])
m2 = MyList([1, 2])
print(bool(m1))
print(bool(m2))
print(len(m2))
```

## Q33 `gate1_oop_basics` / `layer_b`

```python
class Counter:
    count = 0
    def __init__(self):
        self.count += 1  # self.count, not Counter.count

a = Counter()
b = Counter()
print(Counter.count)
print(a.count)
```

## Q34 `gate4_unit_testing` / `layer_a`

```python
import unittest
class T(unittest.TestCase):
    def test_is_none(self):
        self.assertIsNone(None)
    def test_is_not_none(self):
        self.assertIsNotNone(42)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q35 `gate2_inheritance` / `layer_c`

```python
class A:
    def method(self): return 'A'
class B(A):
    def method(self):
        return super().method() + '+B'
class C(B):
    def method(self):
        return super().method() + '+C'
print(C().method())
```

## Q36 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self): self.x = 1
class B(A):
    def __init__(self): self.y = 2
b = B()
print(b.y)
print(b.x)
```

## Q37 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.hstack((np.array([[1],[2]]), np.array([[3],[4]]))).shape)
```

## Q38 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.ones((3,3)).ndim)
```

## Q39 `gate2_inheritance` / `layer_c`

```python
class A:
    def method(self): return 'A'
class B(A): pass
class C(B):
    def method(self): return 'C'
print(C().method())
print(B().method())
```

## Q40 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5, 6])\nb = a\nb[0] = 99\nprint(a[0])
```

## Q41 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_one(self): self.assertEqual(1, 1)
    def test_two(self): self.assertEqual(2, 2)
    def test_three(self): self.assertEqual(1, 2)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q42 `gate5_numpy` / `layer_c`

```python
import numpy as np\na = np.array([1,2,3,4,5,6])\nb = a.reshape(3,2).flatten()\nb[0] = 99\nprint(a[0])
```

## Q43 `gate6_pandas_json` / `layer_c`

```python
df.shape returns what type?
```

## Q44 `gate4_unit_testing` / `layer_b`

```python
import unittest
class T(unittest.TestCase):
    results = []
    def tearDown(self):
        self.results.append('done')
    def test_one(self): pass
    def test_two(self): pass
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(len(T.results))
print(result.result.wasSuccessful())
```

## Q45 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_a(self):
        self.assertTrue(True)
    def test_b(self):
        self.assertTrue(False)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q46 `gate2_inheritance` / `layer_c`

```python
class A:
    def method(self): return 'A'
class B(A):
    def method(self): return 'B'
class C(A):
    def method(self): return 'C'
class D(B, C): pass
print(D().method())
```

## Q47 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_not_equal(self):
        self.assertNotEqual(1, 2)
        self.assertNotEqual('a', 'b')
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q48 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.zeros((2, 3), dtype=int)\na[0] = [1, 2, 3]\nprint(a)
```

## Q49 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.unique(np.array([1,1,2,3,3,4])))
```

## Q50 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __init__(self):
        self.__x = 10
a = A()
a.__x = 99
print(a.__x)
print(a._A__x)
```

## Q51 `gate5_numpy` / `layer_c`

```python
import numpy as np\na = np.array([1,2,3,4,5])\nb = a.copy()\nb[0] = 99\nprint(a[0])
```

---

## Answers

**Q1:** `0
1`  
**Q2:** `Bob - B+`  
**Q3:** `Vehicle: BMW`  
**Q4:** `False`  
**Q5:** `10`  
**Q6:** `True`  
**Q7:** `42`  
**Q8:** `[1.0, 0.0, 3.0]`  
**Q9:** `212.0`  
**Q10:** `3
0`  
**Q11:** `position`  
**Q12:** `99`  
**Q13:** `[1 2 3 4 5 6]`  
**Q14:** `True`  
**Q15:** `(2, 2)`  
**Q16:** `True`  
**Q17:** `<class 'pandas.core.series.Series'>`  
**Q18:** `[0 0 0 1 1]`  
**Q19:** `[3 1 2]`  
**Q20:** `30`  
**Q21:** `False
True`  
**Q22:** `True`  
**Q23:** `[1 3 4 5]`  
**Q24:** `True`  
**Q25:** `False`  
**Q26:** `(2, 3)\n2`  
**Q27:** `[4 6]`  
**Q28:** `B
A`  
**Q29:** `(3, 1)
(3,)`  
**Q30:** `B(1,2)`  
**Q31:** `True
False
False`  
**Q32:** `False
True
2`  
**Q33:** `0
1`  
**Q34:** `2
True`  
**Q35:** `A+B+C`  
**Q36:** `2
AttributeError`  
**Q37:** `(2, 2)`  
**Q38:** `2`  
**Q39:** `C
A`  
**Q40:** `99`  
**Q41:** `3
False`  
**Q42:** `1`  
**Q43:** `tuple`  
**Q44:** `2
True`  
**Q45:** `2
False`  
**Q46:** `B`  
**Q47:** `True`  
**Q48:** `[[1 2 3]\n [0 0 0]]`  
**Q49:** `[1 2 3 4]`  
**Q50:** `99
10`  
**Q51:** `1`  
