# Python Quiz

**50 questions** | Generated: 2026-05-31 08:46

---

## Q1 `gate2_inheritance` / `layer_c`

```python
class A:
    def __init__(self): self.x = 1
class B(A):
    def __init__(self):
        super().__init__()
        self.y = 2
b = B()
print(b.x, b.y)
```

## Q2 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
import json
d = {'x': 1, 'y': 2}
s = json.dumps(d)
d2 = json.loads(s)
print(d2['x'] + d2['y'])
```

## Q3 `gate1_oop_basics` / `layer_c`

```python
class A:
    @classmethod
    def who(cls): return cls.__name__
print(A.who())
```

## Q4 `gate3_encapsulation_dunder` / `layer_c`

```python
class C:
    def __init__(self, v):
        self._v = v
    @property
    def v(self):
        return self._v
    @v.setter
    def v(self, val):
        self._v = val
c = C(1)
c.v = 5
print(c.v)
```

## Q5 `gate2_inheritance` / `layer_b`

```python
class Counter:
    count = 0
    def __init__(self):
        Counter.count += 1

class SpecialCounter(Counter):
    pass

Counter()
SpecialCounter()
SpecialCounter()
print(Counter.count)
print(SpecialCounter.count)
```

## Q6 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([1,2,3]) * 2)
```

## Q7 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.arange(1, 10).reshape(3, 3)\nprint(a[1:, 1:])
```

## Q8 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([[1, 2, 3], [4, 5, 6]])\nprint(a.flatten())\nprint(a.shape)
```

## Q9 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_multi(self):
        self.assertEqual(1, 1)
        self.assertEqual(2, 3)
        self.assertEqual(4, 4)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q10 `gate3_encapsulation_dunder` / `layer_a`

```python
class MyNum:
    def __init__(self, val):
        self.val = val
    def __bool__(self):
        return self.val != 0
print(bool(MyNum(5)))
print(bool(MyNum(0)))
```

## Q11 `gate2_inheritance` / `layer_a`

```python
class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed

d = Dog('Rex', 'Labrador')
print(type(d).__name__)
print(isinstance(d, Animal))
```

## Q12 `gate5_numpy` / `layer_c`

```python
import numpy as np\na = np.array([1,2,3,4,5,6])\nb = a[1:3]\nb[0] = 99\nprint(a[1])
```

## Q13 `gate1_oop_basics` / `layer_b`

```python
class Config:
    debug = False

c = Config()
c.debug = True
print(Config.debug)
print(c.debug)
```

## Q14 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    def test_almost_fail(self):
        self.assertAlmostEqual(0.1, 0.9, places=5)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q15 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3])\nb = np.array([10, 20, 30])\nprint(a + b)
```

## Q16 `gate6_pandas_json` / `layer_c`

```python
type(df['a'][0]) for integer column
```

## Q17 `gate1_oop_basics` / `layer_a`

```python
class Dog:
    def __init__(self, name):
        self.name = name
    def __str__(self):
        return f'Dog: {self.name}'

d = Dog('Rex')
print(d)
```

## Q18 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5, 6])\nb = a\nb[0] = 99\nprint(a[0])
```

## Q19 `gate3_encapsulation_dunder` / `layer_c`

```python
class V:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __add__(self, other):
        return V(self.x + other.x, self.y + other.y)
    def __repr__(self):
        return f'V({self.x},{self.y})'
print(V(1, 2) + V(3, 4))
```

## Q20 `gate3_encapsulation_dunder` / `layer_b`

```python
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __add__(self, other):
        return Vector(self.x + other.x, self.y + other.y)
    def __repr__(self):
        return f'({self.x},{self.y})'
    def __eq__(self, other):
        return self.x == other.x and self.y == other.y
v3 = Vector(1, 2) + Vector(3, 4)
print(v3)
print(v3 == Vector(4, 6))
```

## Q21 `gate6_pandas_json` / `layer_c`

```python
df[['a']] returns what type?
```

## Q22 `gate2_inheritance` / `layer_b`

```python
class A:
    def method(self):
        return 'A'

class B(A):
    def method(self):
        return 'B'

class C(B):
    pass

print(C().method())
```

## Q23 `gate4_unit_testing` / `layer_a`

```python
import unittest
class Stack:
    def __init__(self):
        self.items = []
    def push(self, item):
        self.items.append(item)
    def pop(self):
        return self.items.pop()
    def is_empty(self):
        return len(self.items) == 0
class TestStack(unittest.TestCase):
    def setUp(self):
        self.s = Stack()
    def test_empty(self):
        self.assertTrue(self.s.is_empty())
    def test_push_pop(self):
        self.s.push(1)
        self.assertEqual(self.s.pop(), 1)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q24 `gate1_oop_basics` / `layer_b`

```python
class Foo:
    pass

f = Foo()
print(type(f) == Foo)
print(type(f) == type(Foo))
```

## Q25 `gate1_oop_basics` / `layer_b`

```python
class Dog:
    tricks = []
    def __init__(self, name):
        self.name = name
    def add_trick(self, trick):
        self.tricks.append(trick)

d1 = Dog('Rex')
d2 = Dog('Buddy')
d1.add_trick('sit')
print(d2.tricks)
```

## Q26 `gate1_oop_basics` / `layer_b`

```python
class Person:
    def __init__(self, name):
        self.name = name

p1 = Person('Alice')
p2 = p1
p2.name = 'Bob'
print(p1.name)
```

## Q27 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'val': [10,20,30,40]})
print(df[df['val'] > 20])
```

## Q28 `gate1_oop_basics` / `layer_a`

```python
class Dog:
    species = 'Canis familiaris'
    def __init__(self, name):
        self.name = name

d = Dog('Rex')
d.species = 'Wolf'
print(d.species)
print(Dog.species)
```

## Q29 `gate4_unit_testing` / `layer_c`

```python
import unittest
class T(unittest.TestCase):
    @unittest.skip('skip me')
    def test_skipped(self):
        self.assertEqual(1, 2)
    def test_normal(self):
        self.assertEqual(1, 1)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q30 `gate3_encapsulation_dunder` / `layer_b`

```python
class A:
    def __init__(self):
        self.__x = 10
a = A()
a.__x = 99
print(a.__x)
print(a._A__x)
```

## Q31 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([[1, 2, 3], [4, 5, 6]])\nprint(a.shape)\nprint(a.ndim)
```

## Q32 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.array([[1,2],[3,4]]).sum(axis=1))
```

## Q33 `gate3_encapsulation_dunder` / `layer_b`

```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __eq__(self, other):
        return self.x == other.x and self.y == other.y
p1 = Point(1, 2)
p2 = Point(1, 2)
print(p1 == p2)
print(p1 is p2)
try:
    s = {p1, p2}
except TypeError:
    print('TypeError')
```

## Q34 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5])\nprint(a[a > 3])
```

## Q35 `gate6_pandas_json` / `layer_a`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3]})
df['b'] = df['a'] * 2
print(df)
```

# Q36 `gate3_encapsulation_dunder` / `layer_b`

```python
class Counter:
    def __init__(self):
        self._n = 0
    def __call__(self, step=1):
        self._n += step
        return self._n
    def __repr__(self):
        return f'Counter({self._n})'
c = Counter()
c(); c(); c(5)
print(c)
```

## Q37 `gate4_unit_testing` / `layer_b`

```python
import unittest
class MyList:
    def __init__(self):
        self.data = []
    def add(self, item):
        self.data.append(item)
    def get(self, index):
        return self.data[index]
class T(unittest.TestCase):
    def setUp(self):
        self.lst = MyList()
        self.lst.add(10)
        self.lst.add(20)
    def test_get(self):
        self.assertEqual(self.lst.get(0), 10)
    def test_second(self):
        self.assertEqual(self.lst.get(1), 20)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.testsRun)
print(result.result.wasSuccessful())
```

## Q38 `gate3_encapsulation_dunder` / `layer_b`

```python
class MyList:
    def __init__(self, data):
        self._data = data
    def __len__(self):
        return len(self._data)
    def __bool__(self):
        return len(self._data) > 0
m1 = MyList([])
m2 = MyList([1, 2])
print(bool(m1))
print(bool(m2))
print(len(m2))
```

## Q39 `gate3_encapsulation_dunder` / `layer_b`

```python
class A:
    def __init__(self, x):
        self.__x = x
    def get(self):
        return self.__x
class B(A):
    def get_b(self):
        return self.__x
b = B(5)
print(b.get())
print(b.get_b())
```

## Q40 `gate5_numpy` / `layer_b`

```python
import numpy as np\na = np.array([[1, 2], [3, 4]])\nb = np.array([[5, 6], [7, 8]])\nprint(np.hstack((a, b)))
```

## Q41 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __init__(self, n):
        self.n = n
    def __repr__(self):
        return f'A({self.n})'
items = [A(1), A(2), A(3)]
print(items)
```

## Q42 `gate5_numpy` / `layer_a`

```python
import numpy as np\na = np.array([1, 2, 3, 4, 5])\nprint(a[0])\nprint(a[-1])
```

## Q43 `gate6_pandas_json` / `layer_b`

```python
import pandas as pd
df = pd.DataFrame({'a': [1,2,3]})
print(df['a'][0])
print(type(df['a'][0]))
```

## Q44 `gate2_inheritance` / `layer_b`

```python
class A:
    def __init__(self):
        self.x = 'A'

class B(A):
    def __init__(self):
        self.y = 'B'

b = B()
print(b.y)
print(b.x)
```

## Q45 `gate3_encapsulation_dunder` / `layer_a`

```python
class Person:
    def __init__(self, name):
        self._name = name
p = Person('Alice')
print(p._name)
```

## Q46 `gate6_pandas_json` / `layer_c`

```python
pd.read_csv reads from what?
```

## Q47 `gate3_encapsulation_dunder` / `layer_c`

```python
class A:
    def __init__(self, x):
        self._x = x
    @property
    def x(self):
        return self._x
    @x.deleter
    def x(self):
        del self._x
a = A(5)
del a.x
print(hasattr(a, '_x'))
```

## Q48 `gate2_inheritance` / `layer_b`

```python
class A:
    def __init__(self, x):
        self.x = x

class B(A):
    def __init__(self, x, y):
        super().__init__(x)
        self.y = y
    def total(self):
        return self.x + self.y

b = B(3, 4)
print(b.total())
print(issubclass(type(b), A))
```

## Q49 `gate4_unit_testing` / `layer_b`

```python
import unittest
class T(unittest.TestCase):
    def setUp(self):
        self.log = []
    def tearDown(self):
        self.log.append('tear')
    def test_one(self):
        self.log.append('test')
        self.assertEqual(len(self.log), 1)
result = unittest.main(argv=[''], exit=False, verbosity=0)
print(result.result.wasSuccessful())
```

## Q50 `gate5_numpy` / `layer_c`

```python
import numpy as np\nprint(np.max(np.array([3,1,4,1,5,9])))
```

---

## Answers

**Q1:** `1 2`  
**Q2:** `3`  
**Q3:** `A`  
**Q4:** `5`  
**Q5:** `3
3`  
**Q6:** `[2 4 6]`  
**Q7:** `[[5 6]\n [8 9]]`  
**Q8:** `[1 2 3 4 5 6]\n(2, 3)`  
**Q9:** `False`  
**Q10:** `True
False`  
**Q11:** `Dog
True`  
**Q12:** `99`  
**Q13:** `False
True`  
**Q14:** `False`  
**Q15:** `[11 22 33]`  
**Q16:** `numpy.int64`  
**Q17:** `Dog: Rex`  
**Q18:** `99`  
**Q19:** `V(4,6)`  
**Q20:** `(4,6)
True`  
**Q21:** `DataFrame`  
**Q22:** `B`  
**Q23:** `2
True`  
**Q24:** `True
False`  
**Q25:** `['sit']`  
**Q26:** `Bob`  
**Q27:** `   val
2   30
3   40`  
**Q28:** `Wolf
Canis familiaris`  
**Q29:** `1
True`  
**Q30:** `99
10`  
**Q31:** `(2, 3)\n2`  
**Q32:** `[3 7]`  
**Q33:** `True
False
TypeError`  
**Q34:** `[4 5]`  
**Q35:** `   a  b
0  1  2
1  2  4
2  3  6`  
**Q36:** `Counter(7)`  
**Q37:** `2
True`  
**Q38:** `False
True
2`  
**Q39:** `5
AttributeError`  
**Q40:** `[[1 2 5 6]\n [3 4 7 8]]`  
**Q41:** `[A(1), A(2), A(3)]`  
**Q42:** `1\n5`  
**Q43:** `1
<class 'numpy.int64'>`  
**Q44:** `B
AttributeError: 'B' object has no attribute 'x'`  
**Q45:** `Alice`  
**Q46:** `file path or file-like object`  
**Q47:** `False`  
**Q48:** `7
True`  
**Q49:** `True`  
**Q50:** `9`  
