# Python Quiz

**50 questions** | Generated: 2026-02-11 13:05
**46/50 Qs: 3, 8, 29, 36**

---

## Q1 `gate_8_modules_packages` / `layer_b_concept`

```python
Can you import a module multiple times? Does it re-execute?
```
### Answer:
you can import the module twice but it does not execute as importing it the first time is cached

## Q2 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = 5 / 2
except:
    print('A')
else:
    print('B')

What prints?
```
### Answer:

```console
B
```

## Q3 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
s1 = {1, 2, 3}
s2 = {2, 3, 4}
print(s2 - s1)
```
### Answer:

```console
{1}
```

## Q4 `gate_8_modules_packages` / `layer_b_predict`

```python
# mymodule.py
if __name__ == '__main__':
    print('Running directly')
else:
    print('Imported')

# main.py
import mymodule

What prints?
```
### Answer:

```console
Imported
```
## Q5 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
print(t[1:])
```
### Answer:

```console
(2, 3)
```

## Q6 `gate_6_files_io` / `layer_a`

```python
f = open("new.txt", "x")
f.write("data")
f.close()

File doesn't exist. What happens?
```
### Answer:

file is created and data is written into the file

## Q7 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
print(t[1])
```
### Answer:

```console
2
```

## Q8 `gate_5_list_comprehensions` / `layer_a`

```python
[x*2 for x in [1, 2, 3]]
```
### Answer:

```console
[1, 4, 6]
```

## Q9 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
print(len(a))
```
### Answer:

```console
3
```

## Q10 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [[1, 2], [3, 4]]
print(a[1][0])
```
### Answer:

```console
3
```

## Q11 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
print(a[2:4])
```
### Answer:

```console
[3, 4]
```

## Q12 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3]
a.extend([4, 5])
print(a)
```
### Answer:

```console
[1, 2, 3, 4, 5]
```

## Q13 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    raise ValueError('Custom error')
except ValueError as e:
    print(e)

What prints?
```
### Answer:

```console
Custom error
```

## Q14 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[1:3])
```
### Answer:

```console
[2, 3]
```

## Q15 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = [1, 2]
    x[2]
except IndexError:
    print('Caught')

What prints?
```
### Answer:

```console
Caught
```

## Q16 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "r")
lines = f.readlines()
f.close()

File has 3 lines. What type is lines?
```

### Answer:

```console
<class 'list'>
```
## Q17 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [30, 40, [500, 600, 700], 80], 8]
a[2][2].append(800)
print(a)
```

### Answer:

```console
[1, 2, [30, 40, [500, 600, 700, 800], 80], 8]
```
## Q18 `gate_5_list_comprehensions` / `layer_a`

```python
sentence = "hello world"
[len(w) for w in sentence.split()]
```

### Answer:

```console
[5, 5]
```
## Q19 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [[1, 2], [3, 4]]
print(a)
```

### Answer:

```console
[[1, 2], [3, 4]]
```
## Q20 `gate_8_modules_packages` / `layer_c_speedrun`

```python
What's sys.path?
```

### Answer:

directories that python is looking to execute commands

## Q21 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[3:1])
```

### Answer:

```console
[]
```
## Q22 `gate_4_tuples_sets` / `layer_a`

```python
s1 = {1, 2, 3}
s2 = {3, 4, 5}
print(s1 & s2)
```

### Answer:

```console
{3}
```
## Q23 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    x = 10 / 0
except:
    print('Error')
finally:
    print('Finally')

What prints?
```

### Answer:

```console
Error
Finally
```
## Q24 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = None
    len(x)
except TypeError:
    print('Type error')

What prints?
```

### Answer:

```console
Type error
```
## Q25 `gate_8_modules_packages` / `layer_a_quiz`

```python
from random import choice
print(choice([1, 2, 3]))

What happens?
```

### Answer:

one of the elements from the list is printed

## Q26 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
print(a[1::-1])
```

### Answer:

```console
[2, 1]
```
## Q27 `gate_4_tuples_sets` / `layer_a`

```python
t = tuple([1, 2, 3])
print(t)
```

### Answer:

```console
(1, 2, 3)
```
## Q28 `gate_8_modules_packages` / `layer_b_concept`

```python
What's the difference between import math and from math import sqrt in terms of namespace?
```

### Answer:

```console
import math reserves math name in the namespace and from math import sqrt reserves sqrt, not math
```
## Q29 `gate_5_list_comprehensions` / `layer_a`

```python
{x: x**2 for x in range(5)}
```

### Answer:

```console
{0, 1, 4, 9, 16}
```
## Q30 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = None
    x.append(1)
except AttributeError:
    print('Attribute error')

What prints?
```

### Answer:

```console
Attribute error
```
## Q31 `gate_4_tuples_sets` / `layer_a`

```python
a = 1, 2, 3
print(type(a))
```

### Answer:

```console
<class 'tuple'>
```
## Q32 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
print(type(math))

What prints?
```

### Answer:

```console
<class 'module'>
```
## Q33 `gate_4_tuples_sets` / `layer_b`

```python
s = {}
print(type(s))
```

### Answer:

```console
<class 'dict'>
```
## Q34 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "r")
content = f.read()
f.close()

File exists with 'hello'. What does content contain?
```

### Answer:

```console
hello as a string
```
## Q35 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2]
b = [3, 4]
print(a + b)
```

### Answer:

```console
[1, 2, 3, 4]
```
## Q36 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
del a[1:3]
print(a)
```

### Answer:

```console
[1, 3, 4, 5]
```
## Q37 `gate_4_tuples_sets` / `layer_b`

```python
data = (1, 2, [3, 4])
data[2].append(5)
print(data)
```

### Answer:

```console
(1, 2, [3, 4, 5])
```
## Q38 `gate_8_modules_packages` / `layer_c_speedrun`

```python
How to import multiple items in one line?
```

### Answer:

```console
import module_1, module_2, module_3 (when importing multiple modules/packages)
or
from module/package import method/module, method_1/module_1 ...
```
## Q39 `gate_5_list_comprehensions` / `layer_a`

```python
[x if x > 0 else 0 for x in [1, -2, 3, -4]]
```

### Answer:

```console
[1, 0, 3, 0]
```
## Q40 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
t[0] = 99
print(t)
```

### Answer:

```console
tuples are immutable, will raise an error
```
## Q41 `gate_4_tuples_sets` / `layer_b`

```python
s1 = {1, 2}
s2 = {2, 3}
print(s1 * s2)
```

### Answer:

```console
i don't think it's possible
```
## Q42 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3]
a.insert(1, 99)
print(a)
```

### Answer:

```console
[1, 99, 2, 3]
```
## Q43 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [[1, 2], [3, 4]]
b = a[:]
a.append(99)
print(b)
```

### Answer:

```console
[[1, 2], [3, 4]]
```
## Q44 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    x = {'a': 1}
    print(x['b'])
except KeyError:
    print('Key error')

What prints?
```

### Answer:

```console
Key error
```
## Q45 `gate_8_modules_packages` / `layer_c_speedrun`

```python
Does Python cache imports?
```

### Answer:

yes

## Q46 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[1::2])
```

### Answer:

```console
[2, 4]
```
## Q47 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = 5 / 0
except:
    print('A')
finally:
    print('B')

What prints?
```

### Answer:

```console
A
B
```
## Q48 `gate_5_list_comprehensions` / `layer_a`

```python
[x**2 for x in range(5)]
```

### Answer:

```console
[0, 1, 4, 9, 16]
```
## Q49 `gate_8_modules_packages` / `layer_b_predict`

```python
# mymodule.py
def greet():
    print('Hello')

# main.py
from mymodule import greet
greet()

What prints?
```

### Answer:

```console
Hello
```
## Q50 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
print(sum(a))
```

### Answer:

```console
6
```
---

## Answers

**Q1:** `Yes you can import multiple times, but it doesn't re-execute (cached)`  
**Q2:** `B`  
**Q3:** `{4}`  
**Q4:** `Imported`  
**Q5:** `(2, 3)`  
**Q6:** `File created with 'data'`  
**Q7:** `2`  
**Q8:** `[2, 4, 6]`  
**Q9:** `3`  
**Q10:** `3`  
**Q11:** `[3, 4]`  
**Q12:** `[1, 2, 3, 4, 5]`  
**Q13:** `Custom error`  
**Q14:** `[2, 3]`  
**Q15:** `Caught`  
**Q16:** `list`  
**Q17:** `[1, 2, [30, 40, [500, 600, 700, 800], 80], 8]`  
**Q18:** `[5, 5]`  
**Q19:** `[[1, 2], [3, 4]]`  
**Q20:** `List of directories Python searches for modules`  
**Q21:** `[]`  
**Q22:** `{3}`  
**Q23:** `Error\nFinally`  
**Q24:** `Type error`  
**Q25:** `Prints random element from list`  
**Q26:** `[2, 1]`  
**Q27:** `(1, 2, 3)`  
**Q28:** `import math reserves 'math' namespace; from math import sqrt only reserves 'sqrt'`  
**Q29:** `{0: 0, 1: 1, 2: 4, 3: 9, 4: 16}`  
**Q30:** `Attribute error`  
**Q31:** `<class 'tuple'>`  
**Q32:** `<class 'module'>`  
**Q33:** `<class 'dict'>`  
**Q34:** `hello`  
**Q35:** `[1, 2, 3, 4]`  
**Q36:** `[1, 4, 5]`  
**Q37:** `(1, 2, [3, 4, 5])`  
**Q38:** `from module import item1, item2, item3`  
**Q39:** `[1, 0, 3, 0]`  
**Q40:** `TypeError (tuples immutable)`  
**Q41:** `TypeError`  
**Q42:** `[1, 99, 2, 3]`  
**Q43:** `[[1, 2], [3, 4]]`  
**Q44:** `Key error`  
**Q45:** `Yes (in sys.modules)`  
**Q46:** `[2, 4]`  
**Q47:** `A\nB`  
**Q48:** `[0, 1, 4, 9, 16]`  
**Q49:** `Hello`  
**Q50:** `6`  
