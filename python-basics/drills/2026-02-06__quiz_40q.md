# Python Quiz

**40 questions** | Generated: 2026-02-06 20:42

---

## Q1 `gate_7_exceptions` / `layer_a_quiz`

```python
Can you write code after a bare except: clause?
```
I assume yes? I am not sure what this is asking, but I assume after:
```python
try:
    print(int("b"))
except:
    print(2)
```
Above code would print 2

## Q2 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a)
```
### output would be
```console
[1, 2, 3, 4, 5]
```

## Q3 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
print(a[1::2])
```
### Answer:
```console
[2, 4]
```


## Q4 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = int('10')
except ValueError:
    print('Error')
else:
    print('Success')

What prints?
```
### Answer:

```console
Success
````

## Q5 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = 5 / 2
except:
    print('A')
else:
    print('B')
finally:
    print('C')

What prints?
```
### Answer:

```console
B
C
````
## Q6 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = None
    x.append(1)
except AttributeError:
    print('Attribute error')

What prints?
```
### Answer:

```console
Attribute error
````

## Q7 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[:2])
```
### Answer:

```console
[1, 2]
````

## Q8 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
text = "hello world"
print(text[0:5])
```
### Answer:

```console
hello
````

## Q9 `gate_5_list_comprehensions` / `layer_a`

```python
matrix = [[1, 2], [3, 4]]
[num for row in matrix for num in row]
```
### Answer:

nothing will be shown, but if we were to print the list we are comprehending, it would show:
```console
[1, 2, 3, 4]
````

## Q10 `gate_1_values_types` / `layer_b`

```python
print(not not "")
```
### Answer:

```console
False
````

## Q11 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
print(a[::2])
```
### Answer:

```console
[1, 3, 5]
````

## Q12 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[::-1])
```
### Answer:

```console
[5, 4, 3, 2, 1]
````

## Q13 `gate_2_strings_formatting` / `layer_a`

```python
age = 25
print("Age: {}".format(age))
```
### Answer:

```console
Age: 25
````

## Q14 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
x = 10.5678
print(f"{x:.2f}")
```
### Answer:

```console

````

## Q15 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    f = open('nonexistent.txt')
except FileNotFoundError:
    print('File missing')

What prints?
```

## Q16 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
print("A", "B", sep="")
```

## Q17 `gate_1_values_types` / `layer_c_speedrun`

```python
print(not "")
```

## Q18 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "a")
f.write("more")
f.close()

File has 'hello'. What's in file after?
```

## Q19 `gate_2_strings_formatting` / `layer_b`

```python
print("A")
print("B")
```

## Q20 `gate_2_strings_formatting` / `layer_a`

```python
name = "alice"
print(f"Hello {name.upper()}")
```

## Q21 `gate_1_values_types` / `layer_a_batch_2`

```python
print(True * 5)
```

## Q22 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
print("Line1")
print("Line2", end="")
print("Line3")
```

## Q23 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
print(math.pi)

What prints?
```

## Q24 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    x = {'a': 1}
    print(x['b'])
except KeyError:
    print('Key error')

What prints?
```

## Q25 `gate_1_values_types` / `layer_a_batch_1`

```python
x = 5
print(type(x), type(str(x)))
```

## Q26 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
print(sorted({5, 1, 3}))
```

## Q27 `gate_1_values_types` / `layer_a_batch_2`

```python
x = input()  # user types: hello
print(x, type(x))
```

## Q28 `gate_8_modules_packages` / `layer_c_speedrun`

```python
What's __init__.py for?
```

## Q29 `gate_8_modules_packages` / `layer_c_speedrun`

```python
How to import with alias?
```

## Q30 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    pass
finally:
    print('Finally runs')

What prints?
```

## Q31 `gate_1_values_types` / `layer_c_speedrun`

```python
x = 0
print(bool(x), bool(str(x)))
```

## Q32 `gate_2_strings_formatting` / `layer_a`

```python
print("{1} {0}".format("world", "hello"))
```

## Q33 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = [1, 2, 3]
    print(x[5])
except IndexError:
    print('Out of range')

What prints?
```

## Q34 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = 10 / 2
except ZeroDivisionError:
    print('Error')
else:
    print('Valid')
finally:
    print('End')

What prints?
```

## Q35 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [[1, 2], [3, 4]]
print(a)
```

## Q36 `gate_7_exceptions` / `layer_c_speedrun`

```python
Can you catch SyntaxError with try/except in the same file?
```

## Q37 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [[1, 2], [3, 4]]
a[0] = 99
print(a)
```

## Q38 `gate_8_modules_packages` / `layer_c_speedrun`

```python
import math — what name is added to namespace?
```

## Q39 `gate_8_modules_packages` / `layer_a_quiz`

```python
from math import sqrt
print(sqrt(16))

What prints?
```

## Q40 `gate_3_lists_slicing` / `layer_b_nested`

```python
matrix = [[1, 2, 3], [4, 5, 6]]
print(matrix[1][2])
```

---

## Answers

**Q1:** `Yes`  
**Q2:** `[1, 2, 3, 4, 5]`  
**Q3:** `[2, 4]`  
**Q4:** `Success`  
**Q5:** `B\nC`  
**Q6:** `Attribute error`  
**Q7:** `[1, 2]`  
**Q8:** `hello`  
**Q9:** `[1, 2, 3, 4]`  
**Q10:** `False`  
**Q11:** `[1, 3, 5]`  
**Q12:** `[5, 4, 3, 2, 1]`  
**Q13:** `Age: 25`  
**Q14:** `10.57`  
**Q15:** `File missing`  
**Q16:** `AB`  
**Q17:** `True`  
**Q18:** `hellomore`  
**Q19:** `A\nB`  
**Q20:** `Hello ALICE`  
**Q21:** `5`  
**Q22:** `Line1\nLine2Line3`  
**Q23:** `3.14159...`  
**Q24:** `Key error`  
**Q25:** `<class 'int'> <class 'str'>`  
**Q26:** `[1, 3, 5]`  
**Q27:** `hello <class 'str'>`  
**Q28:** `Makes directory a package, runs on package import`  
**Q29:** `import something as alias`  
**Q30:** `Finally runs`  
**Q31:** `False True`  
**Q32:** `hello world`  
**Q33:** `Out of range`  
**Q34:** `Valid\nEnd`  
**Q35:** `[[1, 2], [3, 4]]`  
**Q36:** `No`  
**Q37:** `[99, [3, 4]]`  
**Q38:** `math`  
**Q39:** `4.0`  
**Q40:** `6`  
