# Python Quiz

**50 questions** | Generated: 2026-02-08 19:27

---

## Q1 `gate_5_list_comprehensions` / `layer_a`

```python
{x for x in [1, 2, 2, 3, 3]}
```

## Q2 `gate_2_strings_formatting` / `layer_a`

```python
print("hello", end="...")
print("world")
```

## Q3 `gate_4_tuples_sets` / `layer_a`

```python
s = {1, 2, 3}
s.add(4)
print(s)
```

## Q4 `gate_4_tuples_sets` / `layer_a`

```python
s = {1, 2, 3}
s.remove(2)
print(s)
```

## Q5 `gate_1_values_types` / `layer_a_batch_1`

```python
print(False * 10)
```

## Q6 `gate_1_values_types` / `layer_a_batch_2`

```python
print(True * 5)
```

## Q7 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
s = "hello"
print(s.replace("l", ""))
```

## Q8 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [30, 40, [500, 600, 700], 80], 8]
print(a[2][2])
```

## Q9 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [3, 1, 4, 1, 5]
a.sort()
print(a)
```

## Q10 `gate_1_values_types` / `layer_b_or_and`

```python
a = 256
b = 256
print(a is b)
```

## Q11 `gate_5_list_comprehensions` / `layer_a`

```python
[x**2 for x in range(5)]
```

## Q12 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[3:1])
```

## Q13 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
del a[2]
print(a)
```

## Q14 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
s = "hello"
print(s.index("l"))
```

## Q15 `gate_1_values_types` / `layer_a_batch_2`

```python
print(bool([False]))
```

## Q16 `gate_1_values_types` / `layer_a_batch_2`

```python
print(2 == True)
```

## Q17 `gate_5_list_comprehensions` / `layer_a`

```python
words = ["a", "", "b", ""]
[w for w in words if w != ""]
```

## Q18 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
x = 10.5678
print(f"{x:.2f}")
```

## Q19 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [3, 4]]
a[3].append(5)
print(a)
```

## Q20 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
print(a.index(2))
```

## Q21 `gate_8_modules_packages` / `layer_b_concept`

```python
What's the difference between import math and from math import sqrt in terms of namespace?
```

## Q22 `gate_7_exceptions` / `layer_c_speedrun`

```python
What exception for calling method on None?
```

## Q23 `gate_2_strings_formatting` / `layer_a`

```python
print("{1} {0}".format("world", "hello"))
```

## Q24 `gate_1_values_types` / `layer_c_speedrun`

```python
print(not "")
```

## Q25 `gate_1_values_types` / `layer_c_speedrun`

```python
x = 5
y = 5
print(x is y)
```

## Q26 `gate_2_strings_formatting` / `layer_b`

```python
print("X" + "Y" + "Z")
```

## Q27 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[2:])
```

## Q28 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
print("{}+{}={}".format(2, 3, 2+3))
```

## Q29 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
print(a[::-1])
```

## Q30 `gate_6_files_io` / `layer_a`

```python
f = open("new.txt", "x")

File already exists. What happens?
```

## Q31 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
t = tuple([1, 2, 3])
print(t)
```

## Q32 `gate_2_strings_formatting` / `layer_c_speedrun`

```python
s = "hello world "
print(s.endswith("world"))
```

## Q33 `gate_6_files_io` / `layer_a`

```python
f = open("new.txt", "x")
f.write("data")
f.close()

File doesn't exist. What happens?
```

## Q34 `gate_7_exceptions` / `layer_c_speedrun`

```python
What exception for list index out of range?
```

## Q35 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
print(math.sqrt(16))

What prints?
```

## Q36 `gate_2_strings_formatting` / `layer_b`

```python
pi = 3.14159
print(f"{pi:.2f}")
```

## Q37 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
print(t.index(2))
```

## Q38 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
import math

Does this execute math module twice?
```

## Q39 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = {'a': 1}
    print(x['b'])
except KeyError:
    print('Key not found')

What prints?
```

## Q40 `gate_1_values_types` / `layer_c_speedrun`

```python
print(not "False")
```

## Q41 `gate_8_modules_packages` / `layer_b_concept`

```python
How do you check what names are available in a module?
```

## Q42 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[:])
```

## Q43 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
del a[1:3]
print(a)
```

## Q44 `gate_1_values_types` / `layer_b_or_and`

```python
print(10 or False)
```

## Q45 `gate_1_values_types` / `layer_c_speedrun`

```python
x = input()  # user types: hello
print(type(x))
```

## Q46 `gate_2_strings_formatting` / `layer_a`

```python
for i in range(3):
    print(i, end=" ")
```

## Q47 `gate_2_strings_formatting` / `layer_b`

```python
text = "hello"
print(text.upper().lower())
```

## Q48 `gate_1_values_types` / `layer_a_batch_2`

```python
x = input()  # user types: hello
print(x, type(x))
```

## Q49 `gate_4_tuples_sets` / `layer_b`

```python
s = set()
print(type(s))
```

## Q50 `gate_4_tuples_sets` / `layer_b`

```python
s = {1, 2, 3}
s.discard(2)
print(s)
```

---

## Answers

**Q1:** `{1, 2, 3}`  
**Q2:** `hello...world`  
**Q3:** `{1, 2, 3, 4}`  
**Q4:** `{1, 3}`  
**Q5:** `0`  
**Q6:** `5`  
**Q7:** `heo`  
**Q8:** `[500, 600, 700]`  
**Q9:** `[1, 1, 3, 4, 5]`  
**Q10:** `True (cached)`  
**Q11:** `[0, 1, 4, 9, 16]`  
**Q12:** `[]`  
**Q13:** `[1, 2, 4, 5]`  
**Q14:** `2`  
**Q15:** `True`  
**Q16:** `False`  
**Q17:** `['a', 'b']`  
**Q18:** `10.57`  
**Q19:** `IndexError`  
**Q20:** `1`  
**Q21:** `import math reserves 'math' namespace; from math import sqrt only reserves 'sqrt'`  
**Q22:** `AttributeError`  
**Q23:** `hello world`  
**Q24:** `True`  
**Q25:** `True`  
**Q26:** `XYZ`  
**Q27:** `[3, 4, 5]`  
**Q28:** `2+3=5`  
**Q29:** `[3, 2, 1]`  
**Q30:** `FileExistsError`  
**Q31:** `(1, 2, 3)`  
**Q32:** `False`  
**Q33:** `File created with 'data'`  
**Q34:** `IndexError`  
**Q35:** `4.0`  
**Q36:** `3.14`  
**Q37:** `1`  
**Q38:** `No (cached in sys.modules)`  
**Q39:** `Key not found`  
**Q40:** `False`  
**Q41:** `dir(module)`  
**Q42:** `[1, 2, 3, 4, 5]`  
**Q43:** `[1, 4, 5]`  
**Q44:** `10`  
**Q45:** `<class 'str'>`  
**Q46:** `0 1 2 `  
**Q47:** `hello`  
**Q48:** `hello <class 'str'>`  
**Q49:** `<class 'set'>`  
**Q50:** `{1, 3}`  
