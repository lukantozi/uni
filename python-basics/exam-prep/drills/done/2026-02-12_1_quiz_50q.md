# Python Quiz

**50 questions** | Generated: 2026-02-12 12:00
**47/50 Qs: 3, 37, 41 (hedged)

---

## Q1 `gate_6_files_io` / `layer_a`

```python
f = open("new.txt", "x")
f.write("data")
f.close()

File doesn't exist. What happens?
```

### Answer:

```console
'data' is written into the "new.txt"
```
## Q2 `gate_8_modules_packages` / `layer_b_concept`

```python
Can you use import *? Is it recommended?
```

### Answer:

```console
you can, though not recommended, since it will pollute the namespace
```
## Q3 `gate_8_modules_packages` / `layer_a_quiz`

```python
from math import sqrt
print(sqrt(16))
print(pi)

What happens?
```

### Answer:

```console
4 # also NameError (open your eyes)
```
## Q4 `gate_8_modules_packages` / `layer_b_predict`

```python
import os
print(os.getcwd())

What does this return?
```

### Answer:

```console
currenct directory path
```
## Q5 `gate_5_list_comprehensions` / `layer_a`

```python
[x if x > 0 else 0 for x in [1, -2, 3, -4]]
```

### Answer:

```console
[1, 0, 3, 0]
```
## Q6 `gate_5_list_comprehensions` / `layer_a`

```python
sentence = "hello world"
[len(w) for w in sentence.split()]
```

### Answer:

```console
[5, 5]
```
## Q7 `gate_5_list_comprehensions` / `layer_a`

```python
[i*j for i in range(1, 4) for j in range(1, 4)]
```

### Answer:

```console
[1, 2, 3, 2, 4, 6, 3, 6, 9]
```
## Q8 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    x = 10 / 0
except:
    print('Error')
finally:
    print('Finally')

What prints?
```

### Answer:

```console
Error
Finally
```
## Q9 `gate_4_tuples_sets` / `layer_a`

```python
s1 = {1, 2, 3}
s2 = {3, 4, 5}
print(s1 & s2)
```

### Answer:

```console
{3}
```
## Q10 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [[1, 2], [3, 4]]
print(a[1])
```

### Answer:

```console
[3, 4]
```
## Q11 `gate_8_modules_packages` / `layer_a_quiz`

```python
from math import sqrt
print(sqrt(16))

What prints?
```

### Answer:

```console
4
```
## Q12 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
a, b, c = t
print(a, b, c)
```

### Answer:

```console
1 2 3
```
## Q13 `gate_8_modules_packages` / `layer_b_concept`

```python
Can you import a module multiple times? Does it re-execute?
```

### Answer:

```console
you can. it does not, first import is cached
```
## Q14 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    raise ValueError('Custom error')
except ValueError as e:
    print(e)

What prints?
```

### Answer:

```console
Custom error
```
## Q15 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, 3]
b = [a, a]
a[0] = 99
print(b)
```

### Answer:

```console
[[99, 2, 3], [99, 2, 3]]
```
## Q16 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = [1, 2, 3]
    print(x[5])
except IndexError:
    print('Out of range')

What prints?
```

### Answer:

```console
Out of range
```
## Q17 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    x = int('abc')
except ValueError:
    print('ValueError')
except Exception:
    print('Exception')

What prints?
```

### Answer:

```console
ValueError
```
## Q18 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
print(dir(math))

What does dir() return?
```

### Answer:

```console
all the methods/items available in math module
```
## Q19 `gate_7_exceptions` / `layer_c_speedrun`

```python
Is finally block required?
```

### Answer:

```console
not required
```
## Q20 `gate_5_list_comprehensions` / `layer_a`

```python
[x*2 for x in [1, 2, 3]]
```

### Answer:

```console
[2, 4, 6]
```
## Q21 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
print(t[1])
```

### Answer:

```console
2
```
## Q22 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = int('abc')
except ValueError:
    print('Invalid')

What prints?
```

### Answer:

```console
Invalid
```
## Q23 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[1::2])
```

### Answer:

```console
[2, 4]
```
## Q24 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = {'a': 1}
    x['b']
except IndexError:
    print('Caught')

Does this catch the error?
```

### Answer:

```console
no, it's a KeyError and we are trying to catch index error
```
## Q25 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [30, 40, [500, 600, 700], 80], 8]
print(a[2][2])
```

### Answer:

```console
[500, 600, 700]
```
## Q26 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "r")
content = f.read()
f.close()

File exists with 'hello'. What does content contain?
```

### Answer:

```console
hello as a string
```
## Q27 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 2, 4]
a.remove(2)
print(a)
```

### Answer:

```console
[1, 3, 2, 4]
```
## Q28 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, [2, 3], 4]
print(a[1][1])
```

### Answer:

```console
3
```
## Q29 `gate_5_list_comprehensions` / `layer_a`

```python
[str(i) for i in range(5)]
```

### Answer:

```console
['0', '1', '2', '3', '4']
```
## Q30 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
a.reverse()
print(a)
```

### Answer:

```console
[3, 2, 1]
```
## Q31 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
x = a.pop()
y = a.pop(0)
print(a, x, y)
```

### Answer:

```console
[2] 3 1 
```
## Q32 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
import math

Does this execute math module twice?
```

### Answer:

```console
no
```
## Q33 `gate_5_list_comprehensions` / `layer_a`

```python
{x: x**2 for x in range(5)}
```

### Answer:

```console
{0: 0, 1: 1, 2: 4, 3: 9, 4: 16}
```
## Q34 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
print(a[1::2])
```

### Answer:

```console
[2, 4]
```
## Q35 `gate_8_modules_packages` / `layer_c_speedrun`

```python
import math — what name is added to namespace?
```

### Answer:

```console
math is reserved
```
## Q36 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "r")
line = f.readline()
f.close()

File has 3 lines. What does line contain?
```

### Answer:

```console
first line
```
## Q37 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [[1, 2], [3, 4]]
b = a
b.append(99)
print(a)
```

### Answer:

```console
[[1, 2], [3, 4]] # wrong, a also will be updated (when is it then when it does not update)
```
## Q38 `gate_7_exceptions` / `layer_a_quiz`

```python
try:
    x = int('10')
except ValueError:
    print('Error')
else:
    print('Success')

What prints?
```

### Answer:

```console
Success
```
## Q39 `gate_8_modules_packages` / `layer_b_concept`

```python
What file makes a directory a package?
```

### Answer:

```console
__init__.py
```
## Q40 `gate_4_tuples_sets` / `layer_b`

```python
t = ((1, 2), (3, 4))
print(t[1])
```

### Answer:

```console
(3, 4)
```
## Q41 `gate_7_exceptions` / `layer_c_speedrun`

```python
What exception for calling method on None?
```

### Answer:

```console
AttributeError (if im wrong, then TypeError, but don't count this as correct)
```
## Q42 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[:2])
```

### Answer:

```console
[1, 2]
```
## Q43 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 2, 4]
print(a.count(2))
```

### Answer:

```console
2
```
## Q44 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [5, 2, 8, 1]
print(max(a), min(a))
```

### Answer:

```console
8 1
```
## Q45 `gate_4_tuples_sets` / `layer_a`

```python
a = 1, 2, 3
print(type(a))
```

### Answer:

```console
<class 'tuple'>
```
## Q46 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
b = a[:]
b[0] = 99
print(a)
```

### Answer:

```console
[1, 2, 3, 4, 5]
```
## Q47 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
print(math.sqrt(16))

What prints?
```

### Answer:

```console
4
```
## Q48 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "r")
lines = f.readlines()
f.close()

File has 3 lines. What type is lines?
```

### Answer:

```console
<class 'list'>
```
## Q49 `gate_4_tuples_sets` / `layer_a`

```python
s1 = {1, 2, 3}
s2 = {3, 4, 5}
print(s1 | s2)
```

### Answer:

```console
{1, 2, 3, 4, 5}
```
## Q50 `gate_7_exceptions` / `layer_c_speedrun`

```python
What exception for invalid int conversion?
```

### Answer:

```console
ValueError
```
---

## Answers

**Q1:** `File created with 'data'`  
**Q2:** `Yes, but not recommended (pollutes namespace with all public names)`  
**Q3:** `4.0 then NameError: pi not defined`  
**Q4:** `Current working directory path`  
**Q5:** `[1, 0, 3, 0]`  
**Q6:** `[5, 5]`  
**Q7:** `[1, 2, 3, 2, 4, 6, 3, 6, 9]`  
**Q8:** `Error\nFinally`  
**Q9:** `{3}`  
**Q10:** `[3, 4]`  
**Q11:** `4.0`  
**Q12:** `1 2 3`  
**Q13:** `Yes you can import multiple times, but it doesn't re-execute (cached)`  
**Q14:** `Custom error`  
**Q15:** `[[99, 2, 3], [99, 2, 3]]`  
**Q16:** `Out of range`  
**Q17:** `ValueError`  
**Q18:** `List of all attributes/methods in module`  
**Q19:** `No`  
**Q20:** `[2, 4, 6]`  
**Q21:** `2`  
**Q22:** `Invalid`  
**Q23:** `[2, 4]`  
**Q24:** `No (KeyError, not IndexError)`  
**Q25:** `[500, 600, 700]`  
**Q26:** `hello`  
**Q27:** `[1, 3, 2, 4]`  
**Q28:** `3`  
**Q29:** `['0', '1', '2', '3', '4']`  
**Q30:** `[3, 2, 1]`  
**Q31:** `[2] 3 1`  
**Q32:** `No (cached in sys.modules)`  
**Q33:** `{0: 0, 1: 1, 2: 4, 3: 9, 4: 16}`  
**Q34:** `[2, 4]`  
**Q35:** `math`  
**Q36:** `First line`  
**Q37:** `[[1, 2], [3, 4], 99]`  
**Q38:** `Success`  
**Q39:** `__init__.py`  
**Q40:** `(3, 4)`  
**Q41:** `AttributeError`  
**Q42:** `[1, 2]`  
**Q43:** `2`  
**Q44:** `8 1`  
**Q45:** `<class 'tuple'>`  
**Q46:** `[1, 2, 3, 4, 5]`  
**Q47:** `4.0`  
**Q48:** `list`  
**Q49:** `{1, 2, 3, 4, 5}`  
**Q50:** `ValueError`  
