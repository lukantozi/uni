# Python Quiz

**50 questions** | Generated: 2026-02-10 14:52
**46/50 Qs: 10, 12, 34, 45

---

## Q1 `gate_4_tuples_sets` / `layer_a`

```python
s1 = {1, 2, 3}
s2 = {3, 4, 5}
print(s1 | s2)
```
### Answer:

```console
{1, 2, 3, 4, 5}
```

## Q2 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
del a[1:3]
print(a)
```
### Answer:

```console
[1, 4, 5]
```

## Q3 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
b = a
b[0] = 99
print(a)
```
### Answer:

```console
[99, 2, 3, 4, 5]
```
## Q4 `gate_7_exceptions` / `layer_b_quiz`

```python
try:
    x = [1, 2, 3]
    print(x[2])
except (IndexError, KeyError):
    print('Caught')

What prints?
```
### Answer:

```console
3
```

## Q5 `gate_8_modules_packages` / `layer_b_predict`

```python
# mymodule.py
def greet():
    print('Hello')

# main.py
from mymodule import greet
greet()

What prints?
```
```console
Hello
```

## Q6 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2]
b = [3, 4]
print(a + b)
```
### Answer:

```console
[1, 2, 3, 4]
```

## Q7 `gate_8_modules_packages` / `layer_b_concept`

```python
What's the difference between a module and a package?
```
### Answer:

module is a collection of functions in a file, and package is a collection of modules, basically a directory

## Q8 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[1])
```

### Answer:

```console
2
```
## Q9 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3, 4, 5]
print(a[1::-1])
```
### Answer:

```console
[2, 1]
```

## Q10 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
*a, b = [1, 2, 3, 4]
print(a, b)
```
### Answer:

```console
[1, 2, 3] [4]
```

## Q11 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[2:])
```
### Answer:

```console
[3, 4, 5]
```
## Q12 `gate_6_files_io` / `layer_a`

```python
f = open("test.txt", "a")
f.write("more")
f.close()

File has 'hello'. What's in file after?
```
### Answer:

```console
hello\n
more # assuming that after hello there is a newline character
```

## Q13 `gate_8_modules_packages` / `layer_c_speedrun`

```python
Difference between module and package?
```
### Answer:

```console
module contains functions, it's basically a file, and a package is a collection of modules, making it a directory
```
## Q14 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [30, 40, [500, 600, 700], 80], 8]
a[2][2].append(800)
print(a)
```
### Answer:

```console
[1, 2, [30, 40, [500, 600, 700, 800], 80], 8]
```

## Q15 `gate_7_exceptions` / `layer_c_speedrun`

```python
What exception for dictionary key not found?
```
### Answer:

```console
KeyError
```

## Q16 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [30, 40, [500, 600, 700], 80], 8]
print(a[2][2])
```
### Answer:

```console
[500, 600, 700]
```
## Q17 `gate_3_lists_slicing` / `layer_b_nested`

```python
nested = [[1, 2], [3, 4], [5, 6]]
print(len(nested))
```
### Answer:

```console
3
```
## Q18 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
t = tuple([1, 2, 3])
print(t)
```
### Answer:

```console
(1, 2, 3)
```
## Q19 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
print(t[1])
```
### Answer:

```console
2
```
## Q20 `gate_4_tuples_sets` / `layer_a`

```python
t = tuple([1, 2, 3])
print(t)
```
### Answer:

```console
(1, 2, 3)
```
## Q21 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [3, 4]]
print(a[3])
```
### Answer:

```console
IndexError
```
## Q22 `gate_5_list_comprehensions` / `layer_a`

```python
[str(i) for i in range(5)]
```
### Answer:

```console
["0", "1", "2", "3", "4"]
```
## Q23 `gate_6_files_io` / `layer_a`

```python
with open("test.txt", "r") as f:
    data = f.read()

Do you need f.close()?
```
### Answer:

no, f.close() is not necessary as with open handles closing the file

## Q24 `gate_8_modules_packages` / `layer_b_concept`

```python
How do you check what names are available in a module?
```
### Answer:

```python
dir(module)
```
## Q25 `gate_8_modules_packages` / `layer_c_speedrun`

```python
from math import sqrt — what name is added?
```
### Answer:
sqrt

## Q26 `gate_4_tuples_sets` / `layer_a`

```python
s = {1, 2, 3}
s.remove(2)
print(s)
```
### Answer:

```console
{1, 3}
```

## Q27 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
t = [(1, 2), (3, 4)]
print(t[1])
```
### Answer:

```console
(3, 4)
```
## Q28 `gate_4_tuples_sets` / `layer_b`

```python
s = {1, 2, 3}
print(len(s))
```
### Answer:

```console
3
```
## Q29 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
a[1:2] = [99, 88]
print(a)
```
### Answer:

```console
[1, 99, 88, 2, 3] # i know that it's wrong andthe correct answer is [1, 99, 88, 3]
```

## Q30 `gate_5_list_comprehensions` / `layer_a`

```python
{x for x in [1, 2, 2, 3, 3]}
```
### Answer:

```console
{1, 2, 3} # if printed
```

## Q31 `gate_8_modules_packages` / `layer_a_quiz`

```python
import math
import math

Does this execute math module twice?
```
### Answer:

no, it caches the first import

## Q32 `gate_8_modules_packages` / `layer_b_concept`

```python
Can you use import *? Is it recommended?
```
### Answer:

you can use, but not recommended as it'll pollute namespace

## Q33 `gate_4_tuples_sets` / `layer_b`

```python
s = {1, 2, 3}
x = s.pop()
print(x, s)
```
### Answer:

```console
1 {2, 3}
```

## Q34 `gate_4_tuples_sets` / `layer_c_speedrun`

```python
a, *b, c = [1, 2, 3, 4, 5]
print(a, b, c)
```
### Answer:

```console
[1] [2, 3, 4] [5]
```

## Q35 `gate_6_files_io` / `layer_a`

```python
f = open("new.txt", "x")
f.write("data")
f.close()

File doesn't exist. What happens?
```
### Answer:

```console
the file is created and 'data' is written into the file
```

## Q36 `gate_4_tuples_sets` / `layer_a`

```python
t = (1, 2, 3)
t[0] = 99
print(t)
```
### Answer:

```console
tuple is immutable, it will throw a TypeError
```

## Q37 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, [3, 4]]
a[3].append(5)
print(a)
```
### Answer:

```console
IndexError
```

## Q38 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[::-1])
```
### Answer:

```console
[5, 4, 3, 2, 1]
```

## Q39 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3]
a.extend([4, 5])
print(a)
```
### Answer:

```console
[1,2,3,4,5]
```

## Q40 `gate_3_lists_slicing` / `layer_a`

```python
a = [1, 2, 3, 4, 5]
print(a[::2])
```
### Answer:

```console
[1, 3, 5]
```

## Q41 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    x = 5 / 2
except:
    print('A')
else:
    print('B')

What prints?
```
### Answer:

```console
B
```

## Q42 `gate_8_modules_packages` / `layer_a_quiz`

```python
from math import sqrt
print(sqrt(16))
print(pi)

What happens?
```
### Answer:

```console
4
NameError
```

## Q43 `gate_7_exceptions` / `layer_c_speedrun`

```python
try:
    f = open('x.txt')
except FileNotFoundError:
    print('Not found')
finally:
    f.close()

Is there a problem?
```
### Answer:

```console
the finally block will be executed regardless of existence of the file, raising an error if it was not opened, and an error was thown
```

## Q44 `gate_5_list_comprehensions` / `layer_a`

```python
[x for x in range(1, 11) if '6' in str(x)]
```
### Answer:

```console
[6]
```

## Q45 `gate_3_lists_slicing` / `layer_c_speedrun`

```python
a = [1, 2, 3]
b = a
a = a + [4]
print(b)
```
### Answer:

```console
[1,2,3,4] -> why is this wrong, don't we point b at where a is? i don't understand
```

## Q46 `gate_3_lists_slicing` / `layer_b_nested`

```python
a = [1, 2, 3]
b = [a, a]
a[0] = 99
print(b)
```
### Answer:

```console
[[0], [1,2,3]]
```

## Q47 `gate_4_tuples_sets` / `layer_a`

```python
s = {1, 2, 3}
print(s[0])
```
### Answer:

```console
sets are not subscribable
```

## Q48 `gate_8_modules_packages` / `layer_c_speedrun`

```python
How to see all names in a module?
```
### Answer:

```console
dir(module)
```

## Q49 `gate_8_modules_packages` / `layer_a_quiz`

```python
from math import pi
print(pi)
print(type(pi))

What prints?
```
### Answer:

```console
3.14159
<class 'float'>
```

## Q50 `gate_4_tuples_sets` / `layer_a`

```python
s = {"car", "boat", "car"}
print(s)
```
### Answer:

```console
{"boat", "car"}
```

---

## Answers

**Q1:** `{1, 2, 3, 4, 5}`  
**Q2:** `[1, 4, 5]`  
**Q3:** `[99, 2, 3, 4, 5]`  
**Q4:** `3`  
**Q5:** `Hello`  
**Q6:** `[1, 2, 3, 4]`  
**Q7:** `Module is a .py file; package is a directory with __init__.py`  
**Q8:** `2`  
**Q9:** `[2, 1]`  
**Q10:** `[1, 2, 3] 4`  
**Q11:** `[3, 4, 5]`  
**Q12:** `hellomore`  
**Q13:** `Module: .py file; Package: directory with __init__.py`  
**Q14:** `[1, 2, [30, 40, [500, 600, 700, 800], 80], 8]`  
**Q15:** `KeyError`  
**Q16:** `[500, 600, 700]`  
**Q17:** `3`  
**Q18:** `(1, 2, 3)`  
**Q19:** `2`  
**Q20:** `(1, 2, 3)`  
**Q21:** `IndexError`  
**Q22:** `['0', '1', '2', '3', '4']`  
**Q23:** `No (with statement handles it)`  
**Q24:** `dir(module)`  
**Q25:** `sqrt`  
**Q26:** `{1, 3}`  
**Q27:** `(3, 4)`  
**Q28:** `3`  
**Q29:** `[1, 99, 88, 3]`  
**Q30:** `{1, 2, 3}`  
**Q31:** `No (cached in sys.modules)`  
**Q32:** `Yes, but not recommended (pollutes namespace with all public names)`  
**Q33:** `Removes arbitrary element`  
**Q34:** `1 [2, 3, 4] 5`  
**Q35:** `File created with 'data'`  
**Q36:** `TypeError (tuples immutable)`  
**Q37:** `IndexError`  
**Q38:** `[5, 4, 3, 2, 1]`  
**Q39:** `[1, 2, 3, 4, 5]`  
**Q40:** `[1, 3, 5]`  
**Q41:** `B`  
**Q42:** `4.0 then NameError: pi not defined`  
**Q43:** `Yes, f is undefined if open fails`  
**Q44:** `[6]`  
**Q45:** `[1, 2, 3]`  
**Q46:** `[[99, 2, 3], [99, 2, 3]]`  
**Q47:** `TypeError (sets not subscriptable)`  
**Q48:** `dir(module)`  
**Q49:** `3.14159...\n<class 'float'>`  
**Q50:** `{'car', 'boat'}`  
